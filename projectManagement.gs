/**
 * @file projectManagement.gs
 * @description โมดูลจัดการโครงการและชมรม
 *
 * ⚠️  หมายเหตุ: ฟังก์ชันทั้งหมดถูกรวมไว้ใน Code.gs (Section 13: PROJECTS)
 *
 *     ฟังก์ชันที่ใช้งานได้:
 *       - getProjectList()                 → ดึงรายการโครงการ/ชมรมทั้งหมด
 *       - saveProjectPlan(projectData)     → บันทึก/แก้ไขโครงการ
 *       - deleteProject(projectId, pass)   → ลบโครงการ (ต้องยืนยันรหัสผ่าน)
 */
